#include <stdio.h>

typedef struct result
{
    int n;
    int d;
};

int main()
{
    int n1, n2, d1, d2;
    char op;
    scanf("%lld / %lld %c %lld / %lld", &n1, &d1, &op, &n2, &d2);
    return 0;
}

result sum(int n1, int n2, int d1, int d2) 
{
    
}
