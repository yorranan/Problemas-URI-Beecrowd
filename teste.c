#include <stdio.h>

int main()
{
    int 
    dia0,
    diaF,
    hora0,
    horaF,
    min0,
    minF,
    seg0,
    segF;

    scanf("Dia %d", &dia0);
    scanf("%d : %d : %d", &hora0, &min0, &seg0);
    scanf("Dia %d", &diaF);
    scanf("%d : %d : %d", &horaF, &minF, &segF);
}