#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int x, aux;
    cin >> x;
    vector<int> v;
    for (int i = 0; i < x; i++)
    {
        cin >> aux;
        v.push_back(aux);
    }
    j = 0;
    while (v[j] != 0)
    {
        v[j]--;
    }
    
    
}